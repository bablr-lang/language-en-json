/* @macrome
 * @generatedby @bablr/macrome-generator-bablr
 * @generatedfrom ./grammar.macro.js#3ac5d265a1c197ab711bf2f9322c4328431406d8
 * This file is autogenerated. Please do not edit it directly.
 * When editing run `npx macrome watch` then change the file this is generated from.
 */
import _applyDecs from "@babel/runtime/helpers/applyDecs2305";
let _initProto, _ExpressionDecs, _ArrayDecs, _ObjectDecs, _StringDecs, _NumberDecs, _BooleanDecs, _NullDecs, _KeywordDecs, _PunctuatorDecs, _ListDecs, _AnyDecs, _AllDecs;
import { re, spam as m } from '@bablr/boot';
import { triviaEnhancer } from '@bablr/helpers/trivia';
import * as productions from '@bablr/helpers/productions';
import { e, eat, eatMatch, match, fail } from '@bablr/helpers/grammar';
import { buildString, buildBoolean, buildIdentifier } from '@bablr/agast-vm-helpers';
import { Node, CoveredBy, AllowEmpty, InjectFrom } from '@bablr/helpers/decorators';
import * as Space from '@bablr/language-en-blank-space';
export const dependencies = {
  Space
};
export const canonicalURL = 'https://github.com/bablr-lang/language-en-json';
export const escapables = new Map(Object.entries({
  b: '\b',
  f: '\f',
  n: '\n',
  r: '\r',
  t: '\t',
  '\\': '\\',
  '/': '/'
}));
export const getCooked = (escapeNode, span, ctx) => {
  let cooked;
  const codeNode = escapeNode.get('code');
  const type = ctx.sourceTextFor(codeNode.get('typeToken'));
  const value = ctx.sourceTextFor(codeNode.get('value'));
  if (!span.startsWith('String')) {
    throw new Error('not implemented');
  }
  if (!type) {
    const match_ = ctx.sourceTextFor(codeNode);
    cooked = escapables.get(match_) || match_;
  } else if (type === 'u') {
    cooked = parseInt(value, 16);
  } else {
    throw new Error();
  }
  return cooked.toString(10);
};
export const grammar = triviaEnhancer({
  triviaIsAllowed: s => s.span === 'Bare',
  *eatMatchTrivia() {
    if (yield match(re`/[ \n\r\t]/`)) {
      yield eat(m`#: <*Space:Space />`);
    }
  }
}, class JSONGrammar {
  static {
    [_initProto] = _applyDecs(this, [[_ExpressionDecs, 2, "Expression"], [_ArrayDecs, 2, "Array"], [_ObjectDecs, 2, "Object"], [Node, 2, "Property"], [_StringDecs, 2, "String"], [[AllowEmpty, Node], 2, "StringContent"], [Node, 2, "EscapeSequence"], [Node, 2, "EscapeCode"], [_NumberDecs, 2, "Number"], [Node, 2, "Integer"], [Node, 2, "UnsignedInteger"], [_BooleanDecs, 2, "Boolean"], [_NullDecs, 2, "Null"], [_KeywordDecs, 2, "Keyword"], [_PunctuatorDecs, 2, "Punctuator"], [_ListDecs, 2, "List"], [_AnyDecs, 2, "Any"], [_AllDecs, 2, "All"]], []).e;
  }
  constructor() {
    _initProto(this);
  }
  *[(_ExpressionDecs = CoveredBy('Element'), _ArrayDecs = [CoveredBy('Expression'), Node], _ObjectDecs = [CoveredBy('Expression'), Node], _StringDecs = [CoveredBy('Language'), Node], _NumberDecs = [CoveredBy('Expression'), Node], _BooleanDecs = [CoveredBy('Expression'), Node], _NullDecs = [CoveredBy('Expression'), Node], _KeywordDecs = [Node, InjectFrom(productions)], _PunctuatorDecs = [Node, InjectFrom(productions)], _ListDecs = [AllowEmpty, InjectFrom(productions)], _AnyDecs = InjectFrom(productions), _AllDecs = InjectFrom(productions), Symbol.for('@bablr/fragment'))]({
    value: {
      productionName
    }
  }) {
    // needed for the trivia plugin
    yield eat(m`<${buildIdentifier(productionName)} />`);
  }
  *Expression() {
    yield eat(m`<Any />`, e([m`<Array '[' />`, m`<Object '{' />`, m`<String '"' />`, m`<Number /-?\d/ span='Number' />`, m`<Null 'null' />`, m`<Boolean /true|false/ />`]));
  }
  *Array() {
    yield eat(m`openToken: <*Punctuator '[' balanced=']' />`);
    yield eat(m`<List />`, e({
      element: m`elements[]$: <Expression />`,
      separator: m`separatorTokens[]: <*Punctuator ',' />`,
      allowTrailingSeparator: e(false)
    }));
    yield eat(m`closeToken: <*Punctuator ']' balancer />`);
  }
  *Object() {
    yield eat(m`openToken: <*Punctuator '{' balanced='}' />`);
    let sep = true;
    yield eatMatch(m`separatorTokens[]: []`);
    yield eatMatch(m`properties[]$: []`);
    while (sep && (yield match(re`/./`))) {
      let suppressGap = yield match(m`<All />`, e([m`<//>`, m`<*Punctuator ':' />`]));
      yield eat(m`properties[]$: <Property />`, null, e({
        suppressGap: e(suppressGap)
      }));
      sep = yield eatMatch(m`separatorTokens[]: <*Punctuator ',' />`);
    }
    yield eat(m`closeToken: <*Punctuator '}' balancer />`);
  }
  *Property() {
    yield eat(m`key$: <String />`);
    yield eat(m`sigilToken: <*Punctuator ':' />`);
    yield eat(m`value$: <Expression />`);
  }
  *String() {
    yield eat(m`openToken: <*Punctuator '"' balanced='"' balancedSpan='String' />`);
    yield eat(m`content: <*StringContent />`);
    yield eat(m`closeToken: <*Punctuator '"' balancer />`);
  }
  *StringContent() {
    let esc, lit;
    do {
      esc = (yield match('\\')) && (yield eat(m`@: <EscapeSequence />`));
      lit = yield eatMatch(re`/[^\r\n\\"\g]+/`);
    } while (esc || lit);
  }
  *EscapeSequence({
    state: {
      span
    },
    ctx
  }) {
    if (!span.startsWith('String')) {
      yield fail();
    }
    yield eat(m`sigilToken: <*Punctuator '\\' openSpan='Escape' />`);
    let match_;
    if (match_ = yield match(re`/[\\/bfnrt"]/`)) {
      const match__ = ctx.sourceTextFor(match_);
      yield eat(m`code: <*Keyword ${buildString(match__)} closeSpan='Escape' />`);
    } else if (yield match('u')) {
      yield eat(m`code: <EscapeCode closeSpan='Escape' />`);
    } else {
      yield fail();
    }
  }
  *EscapeCode() {
    yield eat(m`typeToken: <*Keyword 'u' />`);
    yield eat(m`value$: <*UnsignedInteger />`);
  }
  *Number() {
    yield eat(m`wholePart: <Integer /> { noDoubleZero: true matchSign: '-' }`);
    let fs = yield eatMatch(m`fractionalSeparatorToken: <*Punctuator '.' />`);
    if (fs) {
      yield eat(m`fractionalPart: <Integer />`);
    } else {
      yield eat(m`fractionalPart: null`);
    }
    let es = yield eatMatch(m`exponentSeparatorToken: <*Punctuator /[eE]/ />`);
    if (es) {
      yield eat(m`exponentPart: <Integer /> { matchSign: /[+-]/ }`);
    } else {
      yield eat(m`exponentPart: null`);
    }
  }
  *Integer({
    value: props,
    ctx
  }) {
    const {
      matchSign = null,
      noDoubleZero = false
    } = props && ctx.unbox(props) || {};
    if (matchSign) {
      yield eatMatch(m`signToken: <*Punctuator ${matchSign} />`);
    } else {
      yield eat(m`signToken: null`);
    }
    yield eat(m`value: <*UnsignedInteger />`, e({
      noDoubleZero: buildBoolean(noDoubleZero)
    }));
  }
  *UnsignedInteger({
    value: props,
    ctx
  }) {
    const {
      noDoubleZero = false
    } = props && ctx.unbox(props) || {};
    let firstDigit = ctx.sourceTextFor(yield eat(re`/\d/`));
    if (!noDoubleZero || firstDigit.value !== '0') {
      yield eatMatch(re`/\d+/`);
    }
  }
  *Boolean() {
    yield eat(m`sigilToken: <*Keyword /true|false/ />`);
  }
  *Null() {
    yield eat(m`sigilToken: <*Keyword 'null' />`);
  }
  *Keyword() {}
  *Punctuator() {}
  *List() {}
  *Any() {}
  *All() {}
});